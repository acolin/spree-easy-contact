<% unless @contact.valid? %>
    $("#contact-us-modal").html("<%= escape_javascript(render :partial => "new") %>");
<% else %>
		$("#contact-us-modal").html("<%= escape_javascript(render :partial => "new") %>");
		
		$(':input','#contact-us-modal')
		 .not(':button, :submit, :reset, :hidden')
		 .val('');
		
	  $.notifyBar({ 
			cls: "success", 
			delay: 5000,
			html: "<%= t('message_sent')%>"
		});
<% end  %>